# Pomopet blueprint (番茄小兽)

This folder is a copy-paste starter blueprint for the Pomopet app:
- config hot-update structure (assets/config)
- Drift (SQLite) database schema + DAO queries
- timer service skeleton (endAt-based, notification-friendly)

It is not a full runnable Flutter app yet — it contains the building blocks to paste into a Flutter project.
